// this is comment

/*
  this is multi-line comment.
*/

import module.space

// this is function
function main { f: int, f2: int ->
  print("hello world!")

  // if "a" == "a" then print("hello world!")
  if "a" == "a" or "b" == "b" and "c" == "c" {
      print("hello world!")
  }

  mut a = 3.12 // this is mutable variable
  unmut b = 3 // this is unmutable variable
    
  print(a)
  end a
} -> float
